Description: Load GUI images from Debian's architecture-independent data path
 Install application images below /usr/share instead of embedding them in the
 Python module directory.
Author: Daren Loxley <24845841+2E0LXY@users.noreply.github.com>
Forwarded: not-needed
Last-Update: 2026-09-02
---
--- a/src/ip_analyser/gui.py
+++ b/src/ip_analyser/gui.py
@@ -234,7 +234,8 @@ class Application(tk.Tk):
         self.network_presets = []
         self.favorites_path = Path.home() / ".config" / "advanced-ip-analyser" / "favorites.json"
         try:
-            self.app_icon = tk.PhotoImage(file=Path(__file__).with_name("assets") / "advanced-ip-analyser.png")
+            self.app_icon = tk.PhotoImage(
+                file=Path("/usr/share/advanced-ip-analyser/assets/advanced-ip-analyser.png"))
             self.iconphoto(True, self.app_icon)
         except tk.TclError:
             self.app_icon = None
@@ -481,7 +482,7 @@ class Application(tk.Tk):
 
         notebook = ttk.Notebook(window)
         notebook.pack(fill="both", expand=True, padx=18, pady=(0, 12))
-        assets = Path(__file__).with_name("assets")
+        assets = Path("/usr/share/advanced-ip-analyser/assets")
         window.help_images = []
 
         def add_tab(title: str, body: str, image_name: str | None = None) -> None:
